{
  "name": "@helpers4/landing",
  "version": "1.0.0",
  "description": "helpers4 landing page - built with Qwik",
  "license": "AGPL-3.0",
  "type": "module",
  "scripts": {
    "build": "qwik build --outdir ../../dist",
    "dev": "qwik dev",
    "preview": "qwik preview",
    "clean": "rm -rf dist .qwik"
  },
  "devDependencies": {
    "@builder.io/qwik": "^1.5.0",
    "@builder.io/qwik-cli": "^1.5.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}
